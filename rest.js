import _ from "lodash";

/**
 * An EventEmitter to get a Treehouse students profile.
 * @param username
 * @constructor
 */
function utils(username) {
    console.log(_.size([1,2,3,4]))
}

module.exports = utils;